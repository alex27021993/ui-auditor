# Отчёты прогонов — это РЕЗУЛЬТАТЫ, не инструмент. В репо не храним.
audit-*.xlsx
audit-*.md
# …но слэш-команды аудита — это инструмент, а не отчёты (audit-visual.md и пр.).
!commands/
!commands/*.md

# Временные/служебные
.DS_Store
.~lock.*#
*.code-workspace
.vscode/
